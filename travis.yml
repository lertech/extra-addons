sudo: required
language: python

python:
  - "2.7"

env:
  - VERSION="7.0" ODOO_REPO="odoo/odoo" LINT_CHECK="0"

virtualenv:
  system_site_packages: true

install:
  - git clone https://github.com/amdeb/maintainer-quality-tools.git ${HOME}/maintainer-quality-tools
  - export PATH=${HOME}/maintainer-quality-tools/travis:${PATH}
  - git clone --single-branch --depth=1 https://github.com/Vauxoo/odoo-venezuela.git -b 7.0 ${HOME}/odoo-venezuela-lib
  - export PYTHONPATH=${HOME}/odoo-venezuela-lib:${PYTHONPATH}
  - travis_install_nightly

script:
  - travis_run_tests 7.0

after_success:
  coveralls

